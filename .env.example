# Copiar a .env.local y completar con los datos reales de tu propia base Neon.
# Nunca commitear .env.local — está en .gitignore.

# URL de conexión a Postgres (Neon, plan free). Ejemplo:
# postgresql://usuario:contraseña@host.neon.tech/dbname?sslmode=require
DATABASE_URL=
